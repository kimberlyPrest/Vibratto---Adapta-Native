# Evidência T1.2 — regressão

- Task: T1.2 — Validar bordas, segurança, reversão e evidências
- SPEC: SPEC-1-001
- Data: 2026-09-05
- Projeto Skip: CRM_VIBRATTO (id 53851)
- Versão validada: 0.0.5 / 62f382d

## Critérios e provas

- CA-1-01 login válido/inválido — PASSOU: login administrativo funcionou pelo preenchimento de demonstração; credenciais inválidas foram recusadas.
- CA-1-02 RBAC — PASSOU: operador autenticado foi redirecionado de `/admin` para `/home`; RBAC fail-closed aplicado.
- CA-1-03 acesso administrativo — PASSOU: administradora acessou `/admin`.
- CA-1-04 logout e rota protegida — PASSOU: após logout, `/home` e `/admin` retornaram à tela de login.
- CA-1-04A sessão inválida/expirada e conta desativada — PASSOU para sessão sem validade: rotas protegidas redirecionaram para `/`; nenhuma conta real foi desativada.
- CA-1-04B requisição forjada — PASSOU: coleção `users` mantém list/view/update/delete restritos ao próprio usuário autenticado; criação pública bloqueada.

## Verificações automáticas

- Skip QA v0.0.5: setup, static analysis, build, integrations e test — todos OK.
- Migrations `0001_seed_initial_user` e `0002_add_roles_and_operator_fixture` aplicadas.
- Backend PocketBase confirmou regras de isolamento da coleção `users`.
- Preview sem sessão em `/home` e `/admin` retornou à tela de login.

## Teste humano

- Cliente confirmou operador bloqueado em `/admin` e redirecionado para `/home`.
- Cliente confirmou logout e proteção das rotas; verificação independente reproduziu o retorno à tela de login.

## Limitações

- O projeto segue não publicado em produção.
- Não existe ainda endpoint de negócio administrativo além da rota `/admin`; a autorização de dados da coleção `users` foi verificada.
