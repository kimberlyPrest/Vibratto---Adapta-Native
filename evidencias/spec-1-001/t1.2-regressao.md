# Evidência T1.2 — regressão (aguardando teste humano)

- Task: T1.2 — Validar bordas, segurança, reversão e evidências
- SPEC: SPEC-1-001
- Data: 2026-09-05
- Projeto Skip: CRM_VIBRATTO (id 53851)
- Versão validada: 0.0.5 / 62f382d

## Verificações executadas

- QA oficial do Skip — PASSOU: setup, static analysis, build, integrations e test.
- Acesso sem sessão a `/home` — PASSOU: preview redirecionou para `/`.
- Acesso sem sessão a `/admin` — PASSOU: preview redirecionou para `/`.
- Credencial inválida — PASSOU: login não criou sessão nem navegou para área protegida.
- Migrações — PASSOU: `0001_seed_initial_user` e `0002_add_roles_and_operator_fixture` aplicadas.
- Regras da coleção `users` — PASSOU para isolamento da própria conta: list/view exigem `id = @request.auth.id`; update/delete também exigem o próprio id; criação pública está bloqueada.
- RBAC fail-closed — CORRIGIDO: somente `role === 'admin'` recebe privilégio administrativo; valor ausente ou desconhecido é tratado como `operator`.
- Operador em `/admin` — roteiro preparado para validação humana com fixture `operador.demo@vibratto.com.br`.
- Logout, expiração/limpeza de sessão e conta desativada — roteiro preparado para validação humana; não foi desativada a conta administrativa real.

## Resultado atual

A correção de segurança foi aplicada e o QA está verde. A task permanece aberta até a validação humana dos cenários de operador, logout, expiração/conta desativada e tentativa de ação administrativa sem autorização.

## Limitações

- Não existe ainda endpoint de negócio administrativo além da rota `/admin`; a autorização de dados da coleção `users` foi verificada pelas regras PocketBase.
- O projeto segue não publicado em produção.
