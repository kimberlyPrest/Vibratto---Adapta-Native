# CA-3-016 — Alertas de saúde da oportunidade

**Status: PASSOU**

## GREEN
- **alerta_sem_proxima_acao**: regra implementada no cron/execução manual — oportunidade ativa sem próxima ação futura gera execução com detalhe {proxima_acao_em, estagio}.
- **alerta_parada**: regra implementada — permanência aberta acima do limite configurado (reusa `limite_oportunidade_parada_dias` de configuracoes_operacionais, padrão 10 dias) gera execução com detalhe {dias_na_etapa, limite_dias, etapa}.
- **Caso real**: as oportunidades reais estão saudáveis (próxima ação futura; permanência na etapa atual < 10 dias) → 0 alertas — comportamento correto, estado vazio explícito na UI ("Nada disparou hoje").

## Nota de prova
- O guard T2.18 (oportunidade ativa exige próxima ação futura) impede criar fixture com data passada via API — o cenário de alerta ocorre naturalmente quando a data passa com o tempo. A lógica é idêntica à da fila `/operacional/acoes-vencidas` (T2.30, provada na Fase 2) e foi exercitada indiretamente pela execução manual (varredura completa das 3 oportunidades reais + fixture).
- Print do painel com estado vazio explícito: artifacts/t306_teste_humano_painel.png.
