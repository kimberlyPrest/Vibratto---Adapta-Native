# T9.1 — Green: contadores, tempo por etapa e filas operacionais

**Data:** 2026-09-09
**Versões:** 0.0.73 (implementação), 0.0.74 (correção de deadlock), 0.0.75–0.0.78 (ajustes)
**SPEC:** SPEC-1-009-DERIVADA-contadores-tempo-filas.md

## Implementação

- `pocketbase/migrations/0014_create_operational_counters.js` — coleção append-only `permanencias_negocio`, coleção `configuracoes_operacionais` (seed `limite_oportunidade_parada_dias` = 10), campos `proxima_acao_em`, `proxima_acao_descricao`, `arquivado` em `negocios`.
- `pocketbase/migrations/0015_relax_config_number_required.js` — `valor_numero` aceita 0 (PocketBase trata 0 como blank em number required).
- `pocketbase/hooks/stage_dwell_history.js` — model hooks `onRecordCreate`/`onRecordUpdate` em `negocios`: cria/fecha permanência dentro da transação do próprio save (atômico por construção).
- `pocketbase/hooks/operational_queues.js` — 4 rotas GET `/backend/v1/operacional/{resumo,tempo-por-etapa,acoes-vencidas,paradas}`, todas com `$apis.requireAuth()`.
- `src/pages/Operacional.tsx` + rota `/operacional` + link na Home — painel com contadores, tempo por etapa e filas, com navegação para oportunidades.
- `src/pages/Opportunities.tsx` — campos próxima ação (data/descrição) e arquivado no formulário; badge "Arquivada" na listagem.

## Correção relevante (0.0.74)

Request hook com `e.next()` dentro de `$app.runInTransaction` provocava deadlock (PATCH em mudança de estágio travava sem log). Solução: model hooks, que rodam dentro da transação do save padrão. O hook de migração de etapas voltou ao formato original — o save de cada negócio via `txApp` dispara o model hook e o histórico acompanha na mesma transação.

## Lição técnica (goja/PocketBase)

Date vazio retorna zero value `0001-01-01...` (truthy). Permanência aberta passou a ser detectada por `saiu_em` vazio OU iniciado com `0001-01-01`, no cálculo e nos filtros (`saiu_em = "" || saiu_em ~ "0001-01-01"`).

## Critérios de aceite — resultados observados

| CA       | Resultado | Evidência                                                                                             |
| -------- | --------- | ----------------------------------------------------------------------------------------------------- |
| CA-1-12  | ✅        | resumo: 1 arquivada fora de ativas; 2 ganhas + 1 perdida fora de ativas; 2 ativas                     |
| CA-1-12A | ✅        | permanência aberta calculada até o momento da consulta (proposta: 168s→174s crescente, sem persistir) |
| CA-1-12B | ✅        | tempo-por-etapa soma encerrados (contato_feito 4s, proposta 174s) + aberto (novo 70s)                 |
| CA-1-12C | ✅        | ação em 01/09/2026 entrou na fila (total 1); ação em 10/01/2027 não entrou (total 0)                  |
| CA-1-12D | ✅        | limite 0 → 1 parada listada; limite 10 → fila vazia; admin alterou via PATCH (200)                    |
| CA-1-12E | ✅        | 3 transições criaram/fecharam permanências em sequência correta; histórico preservado                 |
| CA-1-12F | ✅ (parcial) | admin alterou configuração (200); operator não testado por ausência de conta operator ativa — cobrir na T9.2 |
| CA-1-12G | ✅        | rotas exigem auth (401 sem token); filas retornam apenas id/título/estágio/data                       |

## Append-only

- UPDATE em `permanencias_negocio` → 403
- DELETE em `permanencias_negocio` → 403
- CREATE direto por usuário autenticado → 403 (só hooks)

## Estado inválido (duas permanências abertas)

Injeção direta bloqueada pela regra da coleção (403). O cálculo sinaliza `estado_invalido` quando detecta mais de uma permanência aberta por negócio (validado por inspeção do código; caso real não reproduzível sem superuser). Cobertura completa na T9.2.

## QA Skip

- v0.0.73 a v0.0.78: setup, análise estática, build, integrações e testes — todos OK.
