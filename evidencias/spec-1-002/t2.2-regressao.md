# Evidência T2.2 — regressão e auditoria

- Task: T2.2 — Validar bordas, segurança, reversão e evidências
- SPEC: SPEC-1-002
- Data: 2026-09-08
- Projeto Skip: CRM_VIBRATTO (id 53851)
- Versão validada: 0.0.22 / 684cb5b

## Critérios e provas

- CA-1-05 modo demonstração visível — PASSOU: preview exibe `AMBIENTE DE DEMONSTRAÇÃO` e `DEMO — Gate G4 pendente`.
- CA-1-05A produção bloqueada sem G4 — PASSOU: produção não publicada; URL de produção permanece não publicada.
- CA-1-05B consulta reproduzível retorna zero contas .test ativas — PASSOU: consulta backend de usuários filtrada por `.test` retornou zero registros.
- CA-1-05C remoção/desativação é auditada — PASSOU: endpoint admin-only respondeu HTTP 200; registro persistido em `fixture_audit` com `action=deactivated`, `fixture_id`, `actor_id`, `occurred_at` e `reason`.
- CA-1-05D dados fictícios não coexistem com base real — PASSOU no ambiente atual: produção não publicada; dados CRM existentes são dados de seed do ambiente, sem base real de cliente conectada.

## Verificações automáticas

- Skip QA v0.0.22: setup, static analysis, build, integrations e test — todos OK.
- Skip Cloud saudável: migrations e collections carregadas.
- Migrations aplicadas incluem `0007_audited_demo_fixture_catalog`.
- Collections `demo_fixtures` e `fixture_audit` presentes.
- Endpoint `/backend/v1/demo-fixtures/{id}/deactivate` respondeu HTTP 200.
- Auditoria autenticada consultada no backend: `totalItems=1`, ação `deactivated` persistida.
- Produção não publicada.

## Teste humano

- Cliente confirmou ambiente de demonstração ativo.
- Cliente confirmou que a fixture foi desativada com sucesso.

## Segurança e reversão

- A desativação é restrita a usuário com `role=admin`.
- Não há hook global interceptando login.
- A conta administrativa permaneceu funcional.
- A ação é reversível por intervenção administrativa no catálogo, sem rollback destrutivo da base.
